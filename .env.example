# Neon Postgres connection string (never commit the real one)
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require
# Any long random string. Required to run the seeder at /seed
SEED_SECRET=change-me
